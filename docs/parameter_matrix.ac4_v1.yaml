template_id: ac4_v1
description: Conservative AC-4 v1 template modeled only from known XSD input/output structure.
profiles:
  - standard
encode_modes:
  - ac4
input:
  type: ac4
output:
  type: ac4
notes:
  - v1 only models the observed input/audio/ac4 and output/ac4 XML structure.
  - No AC-4 filter parameters are exposed yet because no dedicated AC-4 filter XSD/runtime evidence is captured in this repository.
  - JSON output is intentionally unsupported in v1.
parameters: []
